import { COLORS, FONT_FAMILY } from "./constants";
import {
  border,
  borderRadius,
  fontSize,
  media,
  spacing,
  transition,
  letterSpacing,
} from "./declarations";

const theme = {
  COLORS,
  FONT_FAMILY,
  border,
  borderRadius,
  fontSize,
  media,
  spacing,
  transition,
  letterSpacing,
};

export default theme;
